x = OBJ_Link.x;
y = OBJ_Link.y - 8;
direction = OBJ_Link.direction;
visible = false;
isNotAttacking = true;  // Flag to indicate if the attack is inactive
attackDuration = 10;  // Max Duration of the attack in frames (timequintet)
attackTimer = 0;      // Timer to keep track of attack duration